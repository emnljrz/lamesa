{
  "name": "lamesa",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.34.2",
  "description": "LaMesa monorepo — headless table engine",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}